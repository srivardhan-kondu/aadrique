# Runtime dependencies for the AADRIQUE API.
# Pins match the versions the app was built and tested against.
#
# NOTE: this file previously contained the whole base-image freeze (pandas, boto3,
# stripe, google-genai, litellm from a private URL, …). None of it was imported, and
# the private litellm wheel breaks `pip install` outside the build image. Everything
# listed below is actually imported by server.py / config.py / seed_data.py.

fastapi==0.110.1
starlette==0.37.2
uvicorn[standard]==0.25.0
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
python-multipart==0.0.32

# MongoDB
motor==3.3.1
pymongo==4.6.3
dnspython==2.8.0

# Outbound HTTP (transactional email)
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.14.2
certifi==2026.7.22
idna==3.18
sniffio==1.3.1
typing_extensions==4.16.0
